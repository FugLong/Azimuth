#pragma once

#include <Arduino.h>

/** Settings UI HTML (PROGMEM). Built only in PlatformIO env `azimuth_main_diy` / `azimuth_main_pcb` (see `platformio.ini`). */
extern const char kPortalIndexHtml[] PROGMEM;
