#pragma once
/**
 * Copy this file to secrets.h (same folder) and fill in.
 * include/secrets.h is gitignored — never commit real credentials.
 */

#define WIFI_SSID ""
#define WIFI_PASSWORD ""
#define OPENTRACK_UDP_HOST "192.168.1.1"
